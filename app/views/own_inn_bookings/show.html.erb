
<div class="d-flex mx-5 mt-5 mb-1">
  <%= link_to 'Voltar', bookings_path %>
</div>

<% content_for :booking_actions do %>
  <%= button_to 'Registrar check in', check_in_own_inn_booking_path(@booking), method: :post %>
<% end %>

<%= render partial: 'bookings/info' %>
